# Replace with your domain or use localhost for local dev
:80 {
    reverse_proxy api:8080
}

# For production with a domain, replace the block above with:
# supadash.yourdomain.com {
#     reverse_proxy api:8080
# }
