global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "supadash-api"
    metrics_path: "/v1/metrics"
    static_configs:
      - targets: ["api:8080"]
        labels:
          instance: "supadash"
